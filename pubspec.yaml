name: flutter_formik
description: "A form management library for Flutter inspired by React Formik."
version: 1.0.0
homepage: https://github.com/farajshuaib/flutter_formik
repository: https://github.com/farajshuaib/flutter_formik

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  form_validator: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
flutter:

